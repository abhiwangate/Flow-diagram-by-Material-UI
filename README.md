# Frontend Task: React.js Application with Material UI & React Flow

## Project Overview

This project is a simple **React.js application** that uses **Material UI** for styling and **React Flow** to visualize a list of items as nodes in a flowchart. Users can dynamically add new items to the list.

## Features

- Displays a list of items (tasks or users)
- **Material UI** for modern UI components
- A button to **add new items dynamically**
- **React Flow** for flowchart representation of items

## Technologies Used

- React.js
- Material UI
- React Flow

## Installation and Setup

### Prerequisites:

- Node.js installed (v14 or later)

### Steps:

1. Clone the repository:
   ```sh
   git clone https://github.com/your-username/your-repo.git
   cd your-repo
   ```
2. Install dependencies:
   ```sh
   npm install
   ```
3. Start the development server:
   ```sh
   npm start
   ```
4. Open `http://localhost:3000` in your browser.

## File Structure

```
/src
   ├── components
   │   ├── ItemList.tsx   # Displays list of items
   │   ├── ItemFlow.tsx   # Renders items as a flowchart
   ├── App.tsx           # Main component
   ├── index.tsx         # Entry point
   ├── styles.css        # Styling
```

## How It Works

1. The app loads an initial list of items.
2. Clicking the "Add Item" button dynamically adds a new item.
3. The items are visualized as **nodes** in React Flow.

## Contributions

Feel free to fork and improve this project. Pull requests are welcome!

## License

MIT License

This is done by abhishek wangate

